# Web app only; vLLM runs separately
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic-settings>=2.0.0
openai>=1.0.0

# PDF → images (poppler required for pdf2image; or use pymupdf only)
pdf2image>=1.16.0
pymupdf>=1.24.0
Pillow>=10.0.0
